---
import GLightbox from 'glightbox';
---
<!doctype html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Test GLightbox</title>

    <link href="https://cdn.jsdelivr.net/npm/glightbox/dist/css/glightbox.min.css" rel="stylesheet" />

    <style>
        /* Estilos básicos para centrar el contenido de la prueba */
        body {
            font-family: sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            background-color: #f0f0f0;
        }
        .test-container {
            text-align: center;
            background-color: #fff;
            padding: 2rem;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        .test-link {
            display: inline-block;
            margin-top: 1rem;
            padding: 0.8rem 1.5rem;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }
        .test-link:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="test-container">
        <h1>Prueba de GLightbox</h1>
        <p>Haz clic en la imagen o el enlace para abrir el lightbox.</p>

        <a href="https://placehold.co/600x400/FF5733/FFFFFF?text=Hello+Lightbox" data-glightbox="single-image-test" class="test-link">
             Abrir Imagen de Prueba
        </a>

         </div>

    <script type="module" is:inline>
        // Importa GLightbox
        import GLightbox from 'glightbox';

        console.log('Script de prueba GLightbox ejecutado.');

        // Inicializa GLightbox. Sin un selector específico,
        // GLightbox buscará elementos con data-glightbox en toda la página.
        // O puedes usar un selector si prefieres:
        // const lightbox = GLightbox({ selector: 'a[data-glightbox="single-image-test"]' });
        const lightbox = GLightbox();

        console.log('GLightbox inicializado:', lightbox);

    </script>

</body>
</html>