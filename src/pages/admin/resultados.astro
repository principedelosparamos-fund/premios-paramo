---
import Layout from '../../layouts/Layout.astro';
import ResultadosCliente from '../../components/admin/ResultadosCliente.tsx';
---

<Layout title="Resultados de Votaciones - Premios Páramo">
  <main class="container mx-auto px-4 py-8">
    <h1 class="text-4xl font-bold text-center mb-10">Resultados de Votaciones</h1>

    {/* 
      El componente ResultadosCliente se encarga de toda la lógica de carga de datos en el lado del cliente.
      Esto soluciona el error de permisos al asegurar que la petición a Firebase se hace desde el navegador
      del usuario administrador, que ya está autenticado.
      'client:load' le dice a Astro que renderice este componente de React en el navegador.
    */}
    <ResultadosCliente client:load />

  </main>
</Layout>