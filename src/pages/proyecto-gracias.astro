---
import Layout from "../layouts/Layout.astro";
const { searchParams } = new URL(Astro.request.url);
const nombre = searchParams.get("nombre") || "Postulante";
const obra = searchParams.get("obra") || "tu obra";
---

<Layout>
  <html lang="es">
    <head>
      <title>Gracias por tu postulación</title>
      <style>
        @keyframes fadeZoom {
          0% {
            opacity: 0;
            transform: scale(0.95);
          }
          100% {
            opacity: 1;
            transform: scale(1);
          }
        }

        .fade-in {
          animation: fadeZoom 0.6s ease-out forwards;
        }

        .emoji-bounce {
          animation: bounce 1.2s infinite ease-in-out;
        }

        @keyframes bounce {
          0%,
          100% {
            transform: translateY(0);
          }
          50% {
            transform: translateY(-8px);
          }
        }
      </style>
    </head>
    <body
      class="bg-black-700 text-white font-texto min-h-screen flex items-center justify-center p-6"
    >
      <div
        class="bg-gold-600 text-black rounded-xl shadow-lg p-8 max-w-xl w-full text-center fade-in"
      >
        <div class="text-5xl mb-4 emoji-bounce">🏆</div>
        <h1 class="text-2xl font-bold mb-4">¡Gracias, {nombre}!</h1>
        <p class="text-lg">
          Tu postulación de <strong>{obra}</strong> ha sido registrada con éxito.
        </p>
        <p class="mt-4 text-sm text-black/80">
          Pronto estaremos revisando tu postulación. Gracias por ser parte de
          los Premios Príncipe de los Páramos 2025.
        </p>

        <div class="mt-6 flex justify-center gap-4">
          <a
            href="/"
            class="bg-black-500 text-white px-4 py-2 rounded hover:bg-black-600 transition"
            >Volver al inicio</a
          >
        </div>
      </div>
    </body>
  </html>
</Layout>
