---
import { Image } from 'astro:assets'
import premio from '../../assets/img/premio-pluma.png'
import Button from '../ui/Button.astro'
---

<section
  class="overflow-x-hidden bg-gradient-to-r from-[#191300] to-[#6D5500] py-24"
>
  <div class="container mx-auto grid grid-cols-1 lg:grid-cols-[.5fr_1fr]">
    <div
      id="postula-img"
      class="flex translate-x-[-20px] justify-center opacity-0 transition-all delay-300 duration-700 ease-out"
    >
      <!-- Imagen simple -->
      <Image src={premio} alt="Imagen premio" loading="lazy" />
    </div>
    <div
      id="postula-texto"
      class="translate-x-[20px] opacity-0 transition-all duration-1500 ease-out"
    >
      <div class="flex h-full flex-col items-center justify-center p-6">
        <h2
          class="font-paramo text-center text-2xl leading-10 font-bold text-white sm:text-6xl sm:leading-20"
        >
          Postula tu proyecto a los Premios Príncipe de los Páramos y marca la
          estatuilla con tu nombre.
        </h2>
        <div
          class="grid w-[100%] min-w-[300px] grid-cols-1 gap-8 py-10 md:w-[80%] md:grid-cols-2"
        >
          <Button
            type="primary"
            size="large"
            text="Reglamento"
            href="pdf/reglamento.pdf"
            target="_blank"
            fullWidth
          />
          <Button
            size="large"
            text="Postular"
            href="/proyecto"
            fullWidth
            target="_blank"
          />
        </div>
      </div>
    </div>
  </div>
</section>
<script is:inline>
  const fadeIn = (id, options = {}) => {
    const el = document.getElementById(id)
    if (!el) return
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            el.classList.remove(
              'opacity-0',
              'translate-x-[-20px]',
              'translate-x-[20px]'
            )
            el.classList.add('opacity-100', 'translate-x-0')
          }
        })
      },
      { threshold: 0.6, ...options }
    )
    observer.observe(el)
  }

  fadeIn('postula-img')
  fadeIn('postula-texto')
</script>
